{
  "name": "weighter",
  "version": "1.0.0",
  "description": "Private weight tracker with trend projection. Static site, data in localStorage.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "serve": "python3 -m http.server 8080"
  }
}
